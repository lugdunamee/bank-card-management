<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="004-create-transfers-table" author="cascade">
        <createTable tableName="transfers">
            <column name="id" type="uuid">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="from_card_id" type="uuid">
                <constraints nullable="false"/>
            </column>

            <column name="to_card_id" type="uuid">
                <constraints nullable="false"/>
            </column>

            <column name="amount" type="numeric(19,2)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="timestamp">
                <constraints nullable="false"/>
            </column>

            <column name="created_by" type="varchar(100)">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="varchar(32)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint baseTableName="transfers"
                                 baseColumnNames="from_card_id"
                                 constraintName="fk_transfers_from_card"
                                 referencedTableName="cards"
                                 referencedColumnNames="id"/>

        <addForeignKeyConstraint baseTableName="transfers"
                                 baseColumnNames="to_card_id"
                                 constraintName="fk_transfers_to_card"
                                 referencedTableName="cards"
                                 referencedColumnNames="id"/>

        <createIndex tableName="transfers" indexName="idx_transfers_created_by">
            <column name="created_by"/>
        </createIndex>

        <createIndex tableName="transfers" indexName="idx_transfers_created_at">
            <column name="created_at"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
